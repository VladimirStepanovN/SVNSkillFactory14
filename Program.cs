namespace SVNSkillFactory14
{
    ///<summary>
    ///4.1.10. Измените код в приложении выше таким образом,
    ///чтобы в консоли отобразился результат переменной result.
    ///В окне снизу введите результат выполнения программы.
    ///</summary>
    internal class Program
    {
        static void Main(string[] args)
        {
            var inv = true;
            var result = !inv;
            Console.WriteLine(result);
        }
    }
}